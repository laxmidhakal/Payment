<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androidtutorialshub.loginregister">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.AdmiActivity"></activity>
        <activity android:name=".activities.ReciveActivity" />
        <activity android:name=".activities.AdminActivity" />
        <activity android:name=".activities.PendActivity" />
        <activity android:name=".activities.HisActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".activities.ThirdActivity" />
        <activity android:name=".activities.SecondActivity" />
        <activity android:name=".Second" />
        <activity
            android:name=".Logout"
            android:label="Logout"
            android:parentActivityName=".activities.DashActivity" />
        <activity
            android:name=".About"
            android:label="About"
            android:parentActivityName=".activities.DashActivity" />
        <activity android:name=".activities.DashActivity" />
        <activity
            android:name=".activities.LoginActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.RegisterActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.UsersListActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>